{
  "name": "bhc-field",
  "version": "2.1.3",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "node build/dev-server.mjs",
    "build": "node build/build.mjs",
    "start": "node build/dev-server.mjs dist/client",
    "test": "node --test tests/*.test.mjs",
    "cf:types": "wrangler types --config wrangler.jsonc",
    "cf:types:check": "wrangler types --check --config wrangler.jsonc",
    "cf:migrate:local": "wrangler d1 migrations apply bhc-field --config wrangler.jsonc --local",
    "cf:migrate:list": "wrangler d1 migrations list bhc-field --config wrangler.jsonc --remote",
    "cf:migrate:remote": "wrangler d1 migrations apply bhc-field --config wrangler.jsonc --remote",
    "cf:dry-run": "npm run build && wrangler deploy --config wrangler.jsonc --dry-run"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "wrangler": "^4.134.0"
  }
}
